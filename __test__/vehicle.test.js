describe('Vehicle', () => {
    it('Retrieve Vehicle', async () => {
        // const response = await fetch('http://localhost:3000/api/vehicle');
        // const responseJson = await response.json();
        // expect(response.status).toBe(200);
        expect(2 + 2).toBe(4);
    });
});